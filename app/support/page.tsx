import { Mail, MessageCircle, Phone } from "lucide-react";
import { getSiteSettings } from "@/lib/settings";
export default async function SupportPage(){const s=await getSiteSettings();const wa=s.whatsapp_number.replace(/\D/g,"");return <main className="container mx-auto max-w-4xl px-6 py-14"><h1 className="text-4xl font-bold">Customer support</h1><p className="mt-3 max-w-2xl text-muted-foreground">Ask a question before ordering or send your order code and payment reference if you need help with an existing transaction.</p><div className="mt-10 grid gap-5 md:grid-cols-3"><a className="rounded-2xl border bg-card p-6 hover:border-primary" href={`mailto:${s.contact_email}`}><Mail className="mb-4 h-7 w-7 text-primary"/><h2 className="font-bold">Email</h2><p className="mt-2 break-all text-sm text-muted-foreground">{s.contact_email}</p></a><a className="rounded-2xl border bg-card p-6 hover:border-primary" href={`tel:${s.contact_phone.replace(/[^\d+]/g,"")}`}><Phone className="mb-4 h-7 w-7 text-primary"/><h2 className="font-bold">Call</h2><p className="mt-2 text-sm text-muted-foreground">{s.contact_phone}</p></a><a className="rounded-2xl border bg-card p-6 hover:border-primary" target="_blank" rel="noreferrer" href={`https://wa.me/${wa}`}><MessageCircle className="mb-4 h-7 w-7 text-primary"/><h2 className="font-bold">WhatsApp</h2><p className="mt-2 text-sm text-muted-foreground">Start a support chat</p></a></div><div className="mt-10 rounded-2xl bg-muted p-6"><h2 className="font-bold">For faster help</h2><p className="mt-2 text-muted-foreground">Include your order code, the email used for your account, and a short description. Never send a password, one-time code or full card number.</p></div></main>}
